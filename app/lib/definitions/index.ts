export type MenuItem = { name: string; url: string; order?: string };
export type Article = {
  title: string;
  author: string;
  image: string;
  description: string;
};
export type SocialNetworkItem = {
  name: string;
  icon: React.ReactNode;
  url: string;
};
